export function selectInventoryById (reduxState) {
  console.log('Helo, I select invenotry by Id')
  //return reduxState.carDetails;
  // not sure what reduxState should reference --- update this once database is complete
}